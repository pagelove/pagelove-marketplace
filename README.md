# Pagelove Marketplace

This repository is a minimal Claude-style marketplace for Pagelove plugins. It currently contains a single plugin: `pagelove`, which creates Pagelove apps.

See `marketplace.json` for the plugin listing.
